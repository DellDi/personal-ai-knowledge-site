# 多语言策略

## 语言

- `zh-CN`：当前主语言
- `en`：后续国际化预留

## URL 策略

两个语言都使用显式前缀：

- `/zh-CN/`
- `/en/`

根路径 `/` 是语言入口，不承载主首页内容。

## 内容策略

- 中文内容优先发布。
- 英文路由不能反向主导中文站点表达。
- 内容通过 `lang` 和 `translationKey` 关联。
- 公开查询必须按 `lang` 过滤。

## SEO

公开详情页生成 canonical 和可用翻译的 alternate 链接。没有翻译时，语言切换回对应语言首页，避免指向不存在的详情页。
